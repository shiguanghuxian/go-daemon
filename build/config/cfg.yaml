# go-daemon 守护程序
debug: true
interval: 1s # 这里需配置带单位间隔时间
file_not_exist: true # 此值为true则pid文件不存在启动程序，值为false则忽略
cmd_out: true # 是否输出可执行文件的控制台输出到文件

# 需要守护的pid文件路径,pid 文件只能包含一个pid值
application:
  - pid_path: /app/var/jd25server.pid # pid 路径
    workpath: /app # 工作目录
